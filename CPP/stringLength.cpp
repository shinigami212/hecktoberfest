#include <iostream>
#include <string>

using namespace std;

int main() {
    string input;
    cin >> input;
    int length = input.length();
    cout << "Length of the string: " << length << endl;
    return 0;
}
